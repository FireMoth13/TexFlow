# Texture Pipeline Tool — 使用说明

> PBR 纹理流水线工具箱 | v1.3 | 适用于游戏开发者的纹理处理工作流

---

## 一、安装与启动

### 方式 A：直接运行 .exe（推荐，无需 Java）

1. 打开 `dist/TexturePipelineTool/` 文件夹
2. 双击 `TexturePipelineTool.exe`
3. 程序窗口启动，已内含 JRE + JavaFX，无需任何安装

### 方式 B：源码运行（开发者）

```bash
mvn javafx:run
```

---

## 二、界面概览

```
┌─────────────────────────────────────────────────────────┐
│  📁 导入图片  🗑 清空列表                   工具栏       │
├──────────┬──────────────────────┬───────────────────────┤
│          │                      │🎨 通道打包 (MRAO)     │
│  📋 纹理  │                      │ R: [下拉框]           │
│  列表    │     📷 预览区         │ G: [下拉框]           │
│  (拖拽)  │                      │ B: [下拉框]           │
│          │                      │ ▶ 执行打包            │
│          │                      │───────────────────────│
│          │                      │📐 Mipmap 生成         │
│          │                      │ ▶ 生成 Mipmap         │
│          │                      │───────────────────────│
│          │                      │🧭 法线贴图生成        │
│          │                      │ Strength: [════●═] 2.0│
│          │                      │ 边界: [Clamp ▼]       │
│          │                      │ ▶ 生成法线贴图        │
│          │                      │───────────────────────│
│          │                      │💾 导出                │
│          │                      │ 📥 导出 PNG           │
│          │                      │ WebP 质量: [════●═] 80│
│          │                      │ 📥 导出 WebP          │
│          │                      │ 📥 导出 DDS (BC1)     │
│          │                      │───────────────────────│
│          │                      │⚡ 批量处理            │
│          │                      │ 📂 导入文件夹         │
│          │                      │ 🔄 批量法线           │
│          │                      │ 🔄 批量Mipmap         │
│          │                      │ 🔄 批量导出WebP       │
├──────────┴──────────────────────┴───────────────────────┤
│  处理日志...                                 日志区     │
└─────────────────────────────────────────────────────────┘
```

| 区域 | 功能 |
|------|------|
| **工具栏** | 📁导入图片、🗑清空全部 |
| **左侧面板** | 已加载纹理列表，支持拖拽导入，点击预览 |
| **中央预览** | Canvas 实时像素预览，等比缩放居中 |
| **右侧面板** | 所有处理操作的控制面板 |
| **底部日志** | 实时操作日志、进度、错误信息 |

---

## 三、功能详解

### 3.1 导入纹理

**方法 1 → 点击导入**
1. 点击工具栏 **📁 导入图片**
2. 选择一个或多个纹理文件（支持 `.png` `.jpg` `.jpeg` `.tga` `.bmp`）

**方法 2 → 拖拽导入**
1. 从文件管理器拖拽图片文件到左侧 **"拖拽图片到此处"** 区域
2. 自动过滤并加载支持的格式

**方法 3 → 导入整个文件夹**
1. 点击右侧面板 **📂 导入文件夹**
2. 选择包含纹理的文件夹，自动加载全部图片

**注意**：所有导入的图片会被统一转换为 `ARGB` 格式处理，原文件不受影响。

---

### 3.2 通道打包（MRAO）

**用途**：将 Metallic、Roughness、AO 三张灰度图合并为一张 RGB 纹理，节省显存。

**操作步骤**：
1. 导入三张灰度图（各代表 M / R / AO）
2. 在右侧面板分别选择：
   - **R 通道 (Metallic)** → 选金属度图
   - **G 通道 (Roughness)** → 选粗糙度图
   - **B 通道 (AO)** → 选环境光遮蔽图
3. 未选择的通道自动填白（=1.0，无效果）
4. 点击 **▶ 执行打包**
5. 预览区显示合并结果
6. 点击 **📥 导出** 保存为 PNG

**示例**：只有 Metallic 图时，只需选 R 通道，G、B 自动为白。

---

### 3.3 Mipmap 生成

**用途**：生成纹理的多级缩小版本（1/2 → 1/4 → 1/8 → ... → 1px），避免远处物体渲染时的摩尔纹。

**操作步骤**：
1. 导入一张纹理
2. 点击左侧列表选中该纹理
3. 点击 **▶ 生成 Mipmap**
4. 日志区输出：级数、总内存占用

```
✓ 生成 7 级 Mipmap，总内存 21.3 KB
```

**说明**：Mipmap 链保留在内存中，无直接导出按钮（后续扩展）。

---

### 3.4 法线贴图烘焙

**用途**：从灰度高度图生成切线空间法线贴图（淡紫色），用于 PBR 材质渲染。

**操作步骤**：
1. 导入一张 **灰度高度图**（白=高，黑=低）
2. 调整参数：
   - **凹凸强度 (Strength)**：滑块 0.5 ~ 10.0，默认 2.0。值越大凹凸感越强
   - **边界模式**：
     - `Clamp（边缘复制）`：边缘像素向外复制（适合独立纹理）
     - `Wrap（循环平铺）`：越界像素循环取反侧（适合无缝纹理）
3. 点击 **▶ 生成法线贴图**
4. 预览区显示淡紫色法线贴图
5. 点击 📥 导出保存

**效果验证**：
- 平坦区域 → 淡紫色 `(128, 128, 255)` = 默认法线 `(0, 0, 1)`
- 左低右高斜面 → R 分量 > 128（法线朝右）
- Strength 越大 → 色彩越偏离淡紫 → 凹凸越明显

---

### 3.5 格式转换（PNG → WebP）

**用途**：将 PNG 纹理转为 WebP 格式，减小文件体积（有损压缩）。

**单张导出**：
1. 导入并选中一张纹理
2. 调节 **WebP 压缩质量** 滑块（0~100，默认 80）
   - 95+：近无损，文件较大
   - 75~85：高质量（推荐）
   - 50~70：中等质量
   - <50：高压缩，质量下降明显
3. 点击 **📥 导出当前为 WebP**

**批量导出**：
1. 导入多张纹理（或导入整个文件夹）
2. 调节 WebP 质量
3. 点击 **🔄 批量导出 WebP**
4. 选择输出目录
5. 日志区显示每张进度 `[N/总数]`

---

### 3.6 纹理压缩（BC1 / DXT1）

**用途**：将纹理压缩为 GPU 原生支持的 DXT1 格式，导出 `.dds` 文件。固定压缩比 8:1。

**限制**：宽高必须为 **4 的倍数**（如 256×256、512×1024）。

**操作步骤**：
1. 导入符合尺寸要求的纹理
2. 点击 **📥 导出当前为 DDS (BC1)**
3. 选择保存位置
4. 日志输出压缩比和文件大小

```
✓ DDS(BC1) 导出成功 (12%, 4194304→524288 bytes): output.dds
```

**技术说明**：
- 纯 Java 实现，零外部 C/C++ 依赖
- 算法：4×4 块 → min/max 颜色 → RGB565 量化 → 4 色调色板 → 2-bit 索引
- 兼容所有支持 DXT1 的游戏引擎（Unity、Unreal、Godot 等）

---

### 3.7 多线程批处理

**用途**：对已加载的全部纹理执行同一操作，利用所有 CPU 核心并行处理。

**批量生成法线贴图**：
1. 用 📂导入文件夹 加载多张高度图
2. 设置 Strength 和边界模式
3. 点击 **🔄 批量生成法线贴图**
4. 日志实时输出进度 `[3/10] ✓ height_03.png`

**批量生成 Mipmap**：
1. 导入多张纹理
2. 点击 **🔄 批量生成 Mipmap**

**线程池**：自动使用 `CPU 核心数` 个线程并行处理。

---

## 四、导出格式一览

| 按钮 | 格式 | 用途 |
|------|------|------|
| 📥 导出当前纹理 | PNG | 通用交换格式 |
| 📥 导出当前为 WebP | WebP | 运行时贴图，文件更小 |
| 📥 导出当前为 DDS (BC1) | DDS | GPU 压缩纹理，直接导入引擎 |
| 🔄 批量导出 WebP | WebP | 批量格式转换 |

---

## 五、常见问题

**Q: 窗口启动后闪退？**
A: 确保 `dist/TexturePipelineTool/` 文件夹完整（包含 `runtime/` 子目录）。不要单独移动 `.exe`。

**Q: 法线贴图看起来很平？**
A: 调高 Strength 滑块（5~10），或确保高度图本身有足够的明暗对比度。

**Q: DDS 导出的文件无法在 Photoshop 打开？**
A: Photoshop 需要安装 DDS 插件（如 NVIDIA Texture Tools）。也可以直接用 Unity/Unreal 导入。

**Q: 拖拽导入没反应？**
A: 按住文件拖到左侧 **虚线框区域** 内释放。支持 `.png .jpg .jpeg .tga .bmp`。

**Q: WebP 导出报错？**
A: 确保 `dist/TexturePipelineTool/runtime/` 完整，WebP 原生库已内置在 JAR 中。

---

## 六、技术架构速览

```
引擎层 (engine/)               UI 层 (ui/)
─────────────────          ─────────────────
ChannelPacker.java  ───→   通道打包按钮
MipmapGenerator.java ──→   Mipmap 按钮
NormalMapGenerator.java ─→  法线贴图按钮 + Strength 滑块
PipelineEngine.java   ──→   异步编排 (CompletableFuture)
BatchProcessor.java   ──→   批量处理按钮 + 线程池
FormatConverter.java  ──→   WebP 导出按钮 + 质量滑块
TextureCompressor.java ──→  BC1 导出按钮
```

- **设计原则**：引擎类纯静态方法 + UI 类仅做事件绑定
- **零额外图像依赖**：全部基于 `BufferedImage` 像素级操作
- **测试覆盖**：3 个测试类、21 个用例（JUnit 5）
